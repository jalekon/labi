#include <iostream>
using namespace std;

int main()
{
	int a, b, s, p; //обозначение переменных
	cout << "vvedite zna4enie" << endl; //вывод сообщения на экран
	cin >> a >> b; //ввод данных
	s = a * b; //присваивание нового значения переменной
	p = 2 * (a + b); //присваивание нового значения переменной
	cout << "S=" << s << endl << "P=" << p; //вывод на экран

}	
