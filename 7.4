#include <iostream>

#include <cmath>

using namespace std;

int main()
{
	int y, a, b, c;
	bool x;
	cin >> y;
	c = y % 10;
	a = y / 100;
	b = (y / 10) % 10;
	x = (((a < b) & (b < c)) || ((a > b) & (b > c)));
	cout << x;
}
