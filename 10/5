#include <iostream> 
#include <cmath> 
using namespace std;

int main()
{
	float a;
	int n, i, y;;
	cout << "vvedite 4islo A and N" << endl;
	cin >> a >> n;
	y = 0;
	for (i=0; i<=n;i++)
	{
		y = y + pow(-a,i);
			}
	cout << " summ kvadratov=  " << y;
}
