#include <iostream>
using namespace std;

int main()
{
	int a, b, s, r, p, ch;
	cout << "vvedite NE NULEVIE zna4enie" << endl;
	cin >> a >> b;
	s = pow(a,2) + pow(b, 2);
	r = pow(a, 2) - pow(b, 2);
	p = pow(a, 2) * pow(b, 2);
	ch = pow(a, 2) / pow(b, 2);
	cout << "Summ=" << s << endl << "raznost'=" << r << endl << "proizvedenie'=" << p << endl << "4astnoe'=" << ch;

}
