#include <iostream> 
#include <cmath> 
using namespace std;

int main()
{
	int a, b;
	cout << "vvedite 2 4isla " << endl;
	cin >> a >> b;
	while (a!=b) {
		if (a >= b)
			a = a - b;
		else
			b = b - a;
	}
	cout << "NOD =" << b;
}
