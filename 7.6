#include <iostream>

#include <cmath>

using namespace std;

int main()
{
	int y, a, b, c;
	bool x;
	cout << "vvedite a,b,c"<< endl;
	cin >> a >> b >> c;
	x = ((pow(a, 2) == (pow(b, 2) + pow(c, 2))) || (pow(b, 2) == (pow(a, 2) + pow(c, 2))) || (pow(c, 2) == pow(b, 2) + pow(a, 2)));
	cout << x;
}
