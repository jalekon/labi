#include <iostream>

#include <cmath>

using namespace std;

int main()
{
	int  a, b, c, x, y;
	cout << "a, b, c" << endl;
	cin >> a>>b >>c;
	x = (a/c) * (b/c);
	y=(a*b-x*c*c);
	cout <<"kol-vo kvadratov "<< x << endl<<"ostatok "<<y;
	}
