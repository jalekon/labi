#include <iostream>
using namespace std;

int main()
{
	int a, b, s, r, p, ch;
	cout << "vvedite NE NULEVIE zna4enie" << endl;
	cin >> a >> b;
	s = abs(a) + abs(b);
	r = abs(a) - abs(b);
	p = abs(a) * abs(b);
	ch = abs(a) / abs(b);
	cout << "Summ=" << s << endl << "raznost'=" << r << endl << "proizvedenie'=" << p << endl << "4astnoe'=" << ch;

}
