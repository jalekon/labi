#include <iostream>

#include <cmath>

using namespace std;

int main()

{
int a, b, c, ac, bc, s;
cout << "vvedite to4ki a, b, c, tak 4tobi c bila mejdy nimi " << endl;
cin >> a >> b >> c;
ac = abs(a - c);
bc = abs(b - c);
s = ac * bc;
cout << "proizvedenie = " << s;
}
